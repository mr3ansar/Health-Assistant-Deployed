# ── MediAssist AI — Python Dependencies ────────────────────────────────────
# Pinned for reproducible Streamlit Cloud builds.

# Core
streamlit>=1.35.0,<2.0.0
groq>=0.9.0,<1.0.0

# Local dev only (ignored on Streamlit Cloud — dotenv is optional)
python-dotenv>=1.0.0

# CrewAI multi-agent web search (optional — app degrades gracefully if absent)
crewai>=0.80.0
pydantic
crewai-tools>=0.17.0
duckduckgo-search>=6.1.0
